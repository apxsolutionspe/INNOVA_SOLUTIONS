# Scripts

Carpeta reservada para automatizaciones del proyecto.

Comandos base recomendados:

```bash
npm.cmd run typecheck
npm.cmd run build
npm.cmd run prisma:generate
npm.cmd run prisma:migrate
npm.cmd run prisma:seed
```
